package y3.s1.cse240.quizzes.quiz2;

import y1.s2.Modules.Module2.cse205_p02.src.Student;

public class Quiz2 {
    /* What is a data type?
    - a specialized format of organizing and storing data.
    - a storage location paired with associated symbolic name.
    - a piece of information.
    - a set of primary values and the operations defined on these values.

    Answer:
    a set of primary values and the operations defined on these values.
    */

    /*
    Interpretation of a program is the direct execution of one statement at a time sequentially. True or false?

    Answer:
    True.
    */

    /*
    The syntactic structure of imperative programming languages normally include which of the following units?
    - operators (+-/*%).
    - keywords (int, public).
    - identifiers (name, age, n2).
    - conditional statements. (if, if-else).
    - loop statements. (for, while, do-while).
    - variable declarations. (int n1 = 5), (String name = "Eyad"), (Student s = new Student()).

    Answer:
    - conditional statements.
    - loop statements.
     */

    /*
    What kind of error is in the following line?
    */
    public static void question4() {
//        int a = ((2 * 45) * (6 / 2) hello(4 + 90));
//        return a;
        // What kind of error is in the following line?
        // semantic error, syntactic error or lexical error?
        // answer:
        // semantic error
    }

    /*
    The compiler executes the program. True or false?
    Answer:
    False.
    */

    /*
    Compilation of program is to execute all the statements of the program completely. True or false?
    Answer:
    False.
    */

    /*
    The lexical structure of all programming languages normally include which of the following units?
    - operators (+-/*%).
    - keywords (int, public).
    - identifiers (name, age, n2).
    - conditional statements. (if, if-else).
    - loop statements. (for, while, do-while).
    - variable declarations. (int n1 = 5), (String name = "Eyad"), (Student s = new Student()).

    Answer:
    - operators (+-/*%).
    - keywords (int, public).
    - identifiers (name, age, n2).
     */

    /*
    During compilation, all the statements of a program in a high-level language are converted (translated) to a low-level language (such as assembly language). True or false?

    Answer:
    True
    */

    /*
    Functional programming language are low-level languages.

    Answer:
    False.
    */

    /*
    The semantic structure of imperative programming languages normally includes which of the following validations:
    - type matching.
    - division by zero.
    - a variable name should start with a letter, '$' or '_'.
    - parameters type in a function declaration should match these in function call.
    - statement should end with a ';'
    - unicity.

    Answer:
    - division by zero.
    */

    public static void question10() {
        int x = 5 / 0;
    }
}
